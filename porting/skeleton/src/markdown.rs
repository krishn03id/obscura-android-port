//! markdown.rs — HTML-to-Markdown conversion JS blob.
//!
//! Placeholder: copy this file verbatim from the upstream obscura repo
//! (crates/obscura-js/src/markdown.rs) during the port. It contains a
//! large JavaScript string constant (HTML_TO_MARKDOWN_JS) that implements
//! the Turndown-style HTML→Markdown conversion.

pub const HTML_TO_MARKDOWN_JS: &str = r#"
// Placeholder — replace with actual implementation from upstream obscura-js.
// The real file is ~150 lines of JS embedded as a Rust string constant.
globalThis.__obscura_html_to_markdown = function(html) {
    return html;
};
"#;
